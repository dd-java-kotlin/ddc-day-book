# Junie Project Guidelines Redirection

⚠️ **CRITICAL DIRECTIVE FOR JUNIE AGENT:**
This file contains no project rules. All project-specific context, development rules, patterns, and architectural constraints are centralized inside the `AGENTS.md` file at the repository root — not this file.

### Required Actions for Junie:
1. Open and parse the root `AGENTS.md`, not this file (`.junie/AGENTS.md`).
2. Adopt all instructions, code styles, and workflow rules defined in the root `AGENTS.md`.
3. Treat the root `AGENTS.md` as your primary system prompt and source of truth for this codebase.
